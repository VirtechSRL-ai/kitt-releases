# Kitt releases

Public distribution channel for the Kitt macOS menu-bar app.

- `appcast.xml` on `main` is the Sparkle update feed (`SUFeedURL`).
- Signed/notarized DMGs are attached to GitHub Releases (`vX.Y.Z` tags).
- Releases are published from the private source repo via `scripts/ship.sh --publish`.
